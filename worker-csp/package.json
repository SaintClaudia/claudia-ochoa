{
  "name": "portfolio-csp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "eval:live": "node eval/portfolio-employer-eval.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.22.0",
    "zod": "^4.5.4"
  }
}
